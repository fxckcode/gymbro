services:
  app:
    build:
      context: .
      target: base
    command: pnpm start:dev
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
    ports:
      - "${PORT:-3000}:3000"
      - "9229:9229"
